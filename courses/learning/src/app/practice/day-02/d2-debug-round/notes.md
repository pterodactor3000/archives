# Notes

* signal(ask() - bid()) is a snapshot, so the UI shows yesterday’s spread.
* A subscribe with no teardown keeps the timer after the page is gone.
* [innerHTML] with unknown server data can run attacker HTML.
