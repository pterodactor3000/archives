import { describe, it } from 'vitest';

describe('MockLiveCodingExercise', () => {
  it.todo('adds a valid order with an immutable state update');
  it.todo('rejects an invalid order and preserves existing state');
  it.todo('renders empty state and derived visible quantity');
});
