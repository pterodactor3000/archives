# Angular playground

Angular 22 app on http://127.0.0.1:4200. New apps are zoneless. Tests use Vitest.

```bash
npm run dev:angular
```

From the repo root, `npm run dev` starts this app with the Next.js and TanStack Start playgrounds. Shared quote types and formatting live in `packages/domain`.
